{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Base capability for VIGIL main and companion windows.",
  "windows": ["main", "companion"],
  "permissions": ["core:default"]
}
