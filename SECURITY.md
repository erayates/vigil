# Security Policy

The project is pre-release and not yet accepting production vulnerability reports through a public program.

## Current principles

- local-first data,
- minimal Tauri permissions,
- no remote web content,
- no arbitrary shell execution,
- no bundled secrets,
- dependency review before each beta release.

Do not commit API keys, signing keys, certificates or user databases.
